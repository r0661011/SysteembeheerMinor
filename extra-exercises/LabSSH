ssh/sshd_config:
port 22345
useDNS no
AllowUsers root check

/home/check/.ssh/authorized_keys :
rsa key toevoegen

/home/check :
chmod 700 .ssh
chown -R check:check .ssh

useradd -m check
apt install sshguard
